# Правила боя

Как устроен бой в discord-rpg: механика бит, этапы раунда, формулы урона, ИИ противника. Описывает **текущую реализацию** (`backend/src/Battle/*`, `BattleService`, `BattleController`) — этот документ должен использоваться как опора при проектировании "комнаты боя" (визуальной арены/лобби для PvP и не только).

> **Важно:** этот документ изначально описывал старый **PvP**-алгоритм (одновременный бросок → один подсчёт урона в конце раунда) — теперь устаревший. PvP переведён на ту же боевую систему v2, что PvE/ивенты/турниры: приоритет хода по "преимуществу" и последовательный обмен активациями бит вместо одного подсчёта на весь раунд. См. `docs/COMBAT_V2_DESIGN.md`. Формула `max(0, атака − защита)` из §3 ниже осталась той же, просто применяется **на каждый обмен внутри раунда**, а не один раз на весь раунд — некоторые формулировки ниже (§4, §7) всё ещё описывают старый одновременный алгоритм буквально и требуют мысленной поправки на "по одному обмену", а не переписаны построчно.

---

## 1. Базовая механика: биты

**Бита** (`Bit`) — двусторонняя монета. Сама по себе она ни с кем не связана — это самостоятельное определение (`faceA`/`faceB`/флаги преимущества), на которое ссылаются другие сущности: `CharacterClass.starterBits` (биты, общие для всех персонажей класса) и `Character.purchasedBits` (личные биты, купленные в магазине). У каждой стороны — одна из трёх граней:

| Грань | Эффект |
|---|---|
| `attack` (удар) | Считается в атаку — участвует в подсчёте урона сопернику. |
| `defense` (защита) | Считается в защиту — блокирует чужую атаку. |
| `action` (действие) | Не участвует в подсчёте урона напрямую, но даёт **1 очко действия**: право перевернуть одну уже брошенную биту соперника на другую её грань. |

У каждой грани также есть **множитель** (`multiplierA`/`multiplierB`, по умолчанию 1) — сколько урона/блока/очков действия она реально стоит. Грань с множителем 2 наносит 2 урона (или блокирует 2, или даёт 2 очка действия) одной активацией, а не 1 — работает одинаково для всех трёх граней. См. `docs/COMBAT_V2_DESIGN.md` §1bis.

Полный боевой набор персонажа (`Character::getAllBits()`) — это объединение бит его класса и его личных купленных бит. Если бита класса меняется в админке, это сразу отражается на всех персонажах этого класса (копий на персонажа не делается). Грани каждой конкретной биты фиксированы (например, "удар/защита"), но при броске выпадает случайная из двух — 50/50.

В админке (`/admin` → "Монстры") есть каталог `Monster` — имя, описание, уровень, HP, набор бит, способности, иконка 256×256. При старте PvE-боя (`BattleService::startPveBattle()`) выбирается монстр каталога с уровнем, ближайшим к уровню персонажа, но не выше (`MonsterRepository::findForCharacterLevel()`) — прогрессия сложности по мере роста персонажа; если все монстры в каталоге выше уровня персонажа, берётся монстр с наименьшим уровнем. Оппонент бросает именно биты выбранного монстра (`Monster::$bits`), а бот каждый ход случайно выбирает одну из способностей, назначенных этому монстру (`Monster::$abilities`) — если способностей не назначено, использует Flip.

Если каталог монстров пуст (например, на свежей установке до того, как админ что-то в него добавил) или у выбранного монстра нет ни одной биты, используется старый запасной вариант — фиксированный «Тренировочный голем» из 3 бит (`BattleService::MONSTER_BITS`) с ботом на Flip. Ивенты (`Event`) пока не привязаны к каталогу — используют свои собственные `monsterName`/`monsterHp` и тот же запасной набор бит/Flip, что и раньше.

---

## 2. Виды боя

| Тип | Кто участвует | Стоимость | Награда за победу |
|---|---|---|---|
| **PvE** | персонаж vs тренировочный голем | 1 очко энергии | +10 XP, +5 монет |
| **Ивент** | персонаж vs монстр активного `Event` | бесплатно | +20 XP, +15 монет |
| **Турнир** | персонаж vs персонаж, **симулируется сервером целиком за один вызов** (нет пошагового взаимодействия — см. §6) | — | чемпион турнира: +50 XP, +30 монет (только финальный победитель сетки, не за каждый матч) |
| **PvP-дуэль (`/duel`)** | персонаж vs персонаж | — | фиксированная награда победителю (см. `BattleService`); без ставок. Не засчитывается в недельное задание. Полностью реализовано — см. `docs/BATTLE_ROOM_DESIGN.md`. |

Победа в PvE/ивент-бою также засчитывается в прогресс недельного задания (`QuestService::recordBattleWin`) — но **не** для симулированных турнирных матчей.

---

## 3. Жизненный цикл боя

```
[СТАРТ] → ( [БРОСОК] → [ДЕЙСТВИЯ] → [ПОДСЧЁТ УРОНА] → [ПРОВЕРКА ИСХОДА] )×N → [КОНЕЦ]
              ↑___________________________________________|
                        повтор, пока бой не завершён
```

Бой (`Battle`) состоит из последовательности раундов (`BattleRound` — лог каждого сыгранного раунда: какие грани выпали, сколько урона нанесено).

### Этап 0 — Старт боя

- **PvE**: проверяется `character.energy >= 1`, при успехе списывается 1 энергии. Если энергии нет — `InsufficientEnergyException` → HTTP 409.
- **Ивент**: без проверок, энергия не тратится.
- Создаётся `Battle`: `character`, `opponentName`, `opponentHp`/`opponentMaxHp`, `status = in_progress`, `roundNumber = 0`.

### Этап 1 — Бросок (`throw`)

Одновременно для обеих сторон:
1. Для каждой биты персонажа — случайный выбор одной из двух граней.
2. Для каждой биты соперника (фиксированный шаблон, см. §1) — то же самое.
3. Результат **сохраняется на сервере** в `Battle.pendingPlayerThrows` / `pendingOpponentThrows` (а не отдаётся клиенту "на веру") — это то, что не позволяет клиенту подделать бросок, когда потом присылает выбор целей для "действия".
4. Клиенту возвращается: грани игрока, грани соперника, `playerActionCount` (сколько граней "action" выпало у игрока).

На этом этапе урон **ещё не считается** — игрок должен увидеть доску и решить, куда потратить очки действия.

### Этап 2 — Действия / способности (только если есть `action`-грани)

Это описывает `resolveRound()`/`computeAndApplyRound()` — неинтерактивный путь "весь раунд одним вызовом" (bot text commands: игрок выбирает **одну способность** на раунд и присылает её вместе с целями через `AbilityChoice`; см. §3.1). Интерактивный путь (`throw`/`exchanges/move` — и PvE/ивент, и PvP теперь) выбирает способность **на каждый ход с action-гранью отдельно**, см. §7 ниже.

- Соперник-бот (PvE/ивент) в неинтерактивном режиме всегда использует способность `flip`, цели выбираются **автоматически** эвристикой `BotActionStrategy`:
  1. Сначала переворачивает грани игрока, показывающие `attack` (снижает входящий урон — приоритет выживания).
  2. Затем — грани, показывающие `defense` (снижает защиту игрока → увеличивает урон бота).
  3. Число целей = сколько `action`-граней выпало у соперника.
- Оба выбора применяются **независимо и одновременно** относительно исходного (ещё не изменённого) броска другой стороны — не бывает так, что выбор игрока влияет на то, сколько очков действия доступно сопернику, и наоборот.
- Если игроку не хватает очков действия на выбранную способность (`AbilityType::fixedCost()`, если задан) — способность молча заменяется на `flip` (см. `InteractiveExchangeEngine::applyActionAbility()`); в неинтерактивном пути вместо этого запрос отклоняется с `InsufficientActionPointsException` → HTTP 409.

### 3.1 Боевые способности

Все 4 способности управляются `AbilityResolver`. Стоимость — в очках действия (`action`-гранях), выпавших **в этом же броске**; лишние очки действия, не потраченные на способность, никак не используются (нет "перерасхода" или накопления).

| Способность | Код | Стоимость | Эффект |
|---|---|---|---|
| **Переворот (`flip`)** | `flip` | 1 очко за 1 цель (сколько выпало action-граней — столько целей можно выбрать) | Переворачивает до N уже брошенных бит соперника на другую грань. Единственная способность с целями (`targets` — индексы бит соперника). Поведение не отличается от механики §2 в исходной (до-способностной) версии документа. |
| **Неблокируемый урон (`unblockable_damage`)** | `unblockable_damage` | N (все выпавшие action-очки, без ограничения) | Наносит сопернику N урона **в обход защиты** — прибавляется к итоговому урону уже после формулы §3, не может быть заблокирован гранями `defense`. |
| **Переброс (`reroll`)** | `reroll` | фиксировано 1 | Перебрасывает **все** биты, участвовавшие в этом броске (включая ту, что дала очко действия на саму способность) — на случайную грань каждой (заново 50/50), **до** подсчёта урона. Применяется раньше, чем переворот/неблокируемый урон/зеркало. |
| **Зеркало урона (`damage_mirror`)** | `damage_mirror` | фиксировано 2 | До конца **текущего** раунда соперник получает столько же урона, сколько нанёс сам — итоговый урон сопернику увеличивается на *базовый* (до применения способностей) урон, полученный игроком в этом раунде. |

Неинтерактивный путь (`computeAndApplyRound` → `ExchangeResolver`, `BattleService`) применяет эффекты в этом порядке:
1. `applyPreDamage` — `reroll` (если выбран) переигрывает броски **до** всего остального.
2. Подсчёт `flip`-целей (`effectiveFlipTargets`) и обычный расчёт урона (формула §3, без изменений).
3. `applyPostDamage` — к результату добавляются `unblockable_damage` и `damage_mirror`; зеркало берёт значение урона **до** применения обеих способностей (снимок), поэтому если обе стороны одновременно выбрали `damage_mirror`, они не влияют друг на друга рекурсивно.

Способность выбирается заново каждый раунд — она не "включена" постоянно, эффект `damage_mirror` действует строго до конца того раунда, в котором была выбрана (несмотря на формулировку "до конца текущего раунда" в исходном ТЗ — раунд здесь один "бросок→подсчёт", а не весь бой).

### Этап 3 — Подсчёт урона

После применения всех переворотов финальные грани раскладываются на подсчёт:

```
playerAttack   = кол-во граней "attack" у игрока (после переворотов)
playerDefense  = кол-во граней "defense" у игрока
opponentAttack = кол-во граней "attack" у соперника
opponentDefense= кол-во граней "defense" у соперника

damageToOpponent = max(0, playerAttack  − opponentDefense)
damageToPlayer   = max(0, opponentAttack − playerDefense)
```

Грани `action` в этом подсчёте не участвуют вообще (ни в атаку, ни в защиту) — их ценность целиком в перевороте чужой биты на этапе 2.

### Этап 4 — Применение урона и проверка исхода

1. `character.hp -= damageToPlayer`, `opponent.hp -= damageToOpponent` (не уходят ниже 0).
2. `roundNumber += 1`, в БД пишется `BattleRound` (грани обеих сторон + нанесённый урон — полный лог для истории/аналитики, виден в `/admin`).
3. Исход:
   - `opponent.hp <= 0` → `status = won`. Начисляются XP/монеты (см. §2), засчитывается прогресс недельного задания.
   - `character.hp <= 0` (и `opponent.hp > 0`) → `status = lost`.
   - Иначе → бой продолжается, возврат к этапу 1 (новый бросок).
4. HP персонажа **не восстанавливается автоматически** между боями — если персонаж пришёл на бой уже раненым, он стартует с текущим HP. Лечения/отдыха в игре пока нет.

### Особые случаи

- **Одновременный нокаут** (оба HP ≤ 0 в одном раунде) — победа присуждается игроку (`opponentHp <= 0` проверяется первым в коде).
- **Затяжной бой без явного разрешения**: в режимах "авто-игры" (текстовые команды бота `/pve`, `/event-fight`, а также симуляция турнирного матча) действует потолок в **30 раундов** — если за 30 раундов никто не умер, бой останавливается, статус остаётся `in_progress`, отображается как "ничья/бой затянулся". В интерактивном режиме через Activity потолка на число раундов нет — игрок может играть сколько угодно раундов подряд, вручную нажимая "Бросить биты".

---

## 4. API-эндпоинты боя (что уже есть)

| Метод | Путь | Назначение |
|---|---|---|
| `POST` | `/api/battles/pve` | Начать PvE-бой (списывает энергию). |
| `POST` | `/api/events/active/battle` | Начать бой с монстром активного ивента (бесплатно). |
| `GET` | `/api/battles/my-active-pvp` | Есть ли у меня активная PvP-дуэль (для решения, что показать при входе в Activity). |
| `GET` | `/api/battles/{id}` | Текущее состояние боя (HP сторон, статус, `hasPendingThrow`; для PvP — с учётом зрителя). |
| `POST` | `/api/battles/{id}/join` | PvP: принять приглашение и/или отметиться "готов". Когда готовы оба — бой переходит в `in_progress`, автоматически бросается первый раунд. |
| `POST` | `/api/battles/{id}/decline` | PvP: отклонить приглашение. |
| `POST` | `/api/battles/{id}/throw` | Этап 1 — бросить биты обеих сторон. Дополнительно возвращает `turn` (`lead`/`respond`/`wait`/`over`) и `incomingMove`, если кто-то уже сходил первым (см. `docs/COMBAT_V2_DESIGN.md` §7-8). Для PvP `wait` означает "сейчас ходит соперник". |
| `POST` | `/api/battles/{id}/exchanges/move` | Интерактивный пошаговый бой — PvE/ивент и PvP одинаково. Тело `{indices, ability?, targets?}` — один ход (сервер сам решает, ведёте вы или отвечаете, по своей стороне `battle.pendingExchangeState`). Пустой `indices` при ответе = пропустить (полный урон от входящего хода); пустой `indices`, когда ваш ход вести — PvP-специфично, отказ от инициативы в этом обмене (инициатива переходит другой стороне). |
| `GET` | `/api/battles/{id}/rounds/latest` | PvP: последний посчитанный раунд — так сторона, ждавшая соперника, узнаёт результат. |

Для всех режимов `throw`/`exchanges/move` — единый источник истины для UI "комнаты боя" (`ArenaScreen`): экран зовёт `throw`, показывает расклад и чей ход, затем игрок раз за разом выбирает биты и зовёт `exchanges/move` — по одному вызову на каждый ход, пока раунд не завершится (`roundComplete: true` в ответе). Разница только в том, кто играет другую сторону: для PvE/ивента — бот, разыгрывающий свой ход синхронно в том же запросе; для PvP — второй живой игрок через свой отдельный запрос, поэтому клиент того, чей ход сейчас `wait`, поллит `GET /{id}` (поле `exchange`), пока не увидит смену хода или завершение раунда. Если сторона, чья очередь ходить, не прислала ход вовремя — `roundDeadlineAt` (`BattleService::applyPvpMoveTimeoutIfExpired()`) лениво считает это пропуском: пропуск ответа = полный урон от входящего хода, пропуск инициативы = ход переходит другой стороне.

Bot-эндпоинты (`/api/bot/battles/{discordId}/pve/auto`, `/api/bot/events/active/battle/auto/{discordId}`) — без пошагового взаимодействия: сервер вызывает `BattleService::resolveRound()` (весь раунд одним PHP-вызовом, способность `flip` без целей для игрока) в цикле, пока бой не закончится или не будет достигнут потолок в 30 раундов. Это текстовый режим для Discord-команд, не для Activity, и не использует интерактивный протокол выше.

---

## 5. Формула боя в псевдокоде (для справки)

```
function playRound(character, opponent):
    playerThrows   = [throwRandomFace(bit) for bit in character.bits]
    opponentThrows = [throwRandomFace(bit) for bit in opponent.bits]

    playerActionCount   = count(playerThrows, "action")
    opponentActionCount = count(opponentThrows, "action")

    # игрок присылает свои цели отдельным запросом (resolve), бот — вычисляет сам
    opponentThrows = flip(opponentThrows, playerActionTargets[:playerActionCount])
    playerThrows   = flip(playerThrows, botStrategy(playerThrows, opponentActionCount))

    dmgToOpponent = max(0, count(playerThrows,"attack")   - count(opponentThrows,"defense"))
    dmgToPlayer   = max(0, count(opponentThrows,"attack") - count(playerThrows,"defense"))

    opponent.hp   -= dmgToOpponent
    character.hp  -= dmgToPlayer

    if opponent.hp <= 0:  return WON
    if character.hp <= 0: return LOST
    return CONTINUE
```

---

## 6. Турнирные бои — важное отличие

Турнирные матчи (`TournamentService::simulateMatch`) используют **тот же `ExchangeResolver`**, что и неинтерактивный PvE/ивент-автобой, но:

- Обе стороны — реальные персонажи, у обеих HP берётся `maxHp` (не текущий поврежденный HP — бой всегда начинается "по полному баку", независимо от состояния персонажа в общем мире).
- **Обе** стороны используют фиксированный выбор способности `flip` (ни одна сторона не интерактивна, поэтому без реального выбора игрока).
- Бой играется **сразу до конца** (до 30 раундов) одним вызовом, без пошагового `throw`/`exchanges/move` — результат матча известен мгновенно после запуска турнира.
- Если 30 раундов не дали победителя — судьба матча решается по остатку HP (`aHp >= bHp ? a : b`).

Это **не** живой PvP — просто "быстрый прогон" статов друг против друга, чтобы сетка турнира вообще могла существовать без реализации синхронизации двух живых игроков.

---

## 7. PvP-комната боя

Полностью реализована — второй живой участник вместо бота, играющий по тому же интерактивному пошаговому протоколу, что PvE/ивент (`InteractiveExchangeEngine`, см. `docs/COMBAT_V2_DESIGN.md` §7-8), а не старой одновременной схеме. Комната ожидания (`join`/`decline`, статус `waiting` → `in_progress`) и сериализация с учётом того, кто из двух игроков смотрит (`BattleSerializer::battleForViewer`/`throwResultForViewer`/`exchangeMoveResultForViewer`/`roundForViewer`) — без изменений. Синхронизация между двумя живыми клиентами теперь через поллинг `GET /{id}` (поле `exchange`), а не ожидание "обе стороны прислали выбор"; лениво проверяемый тайм-аут хода — на уровне отдельного обмена (`applyPvpMoveTimeoutIfExpired`), а не всего раунда. Архитектура лобби/готовности — в `docs/BATTLE_ROOM_DESIGN.md` (раздел про сам механизм подсчёта раунда там устарел, см. заметку в начале того документа).

Формулы урона и грани бит (§3) в PvP не отличаются от PvE — единственная разница в том, откуда берётся выбор способности и хода "второй стороны": не бот-эвристика, а реальный второй игрок через тот же `exchanges/move`, что и первая сторона.
